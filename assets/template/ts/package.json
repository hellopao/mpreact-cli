{
  "name": "example",
  "version": "0.0.1",
  "description": "mpreact ts example",
  "main": "index.js",
  "scripts": {
    "build": "mpreact-cli build -s src -d dist",
    "dev": "mpreact-cli build -s src -d dist -w"
  },
  "keywords": [
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mpreact": "^0.0.1"
  },
  "devDependencies": {
  }
}
